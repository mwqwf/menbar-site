# موقع منبر ادكصهك (يُحمَّل تلقائياً في كل جلسة)

> الردود بالعربية دائماً.

## أين أنت — وخريطة المشروع

هذا **مستودع الموقع المنشور**: `minbar-adkassahk.vercel.app` يُنشر من هنا
(مشروع Vercel `prj_xgQzb3NbkMtfPmJGlwiEzlXdD6H8`)، والدفع إلى `main` ينشر.

| المستودع | ما فيه |
|---|---|
| **`mwqwf/menbar-site`** (هنا) | الموقع: تصيير من الخادم `api/render.js`، ومشغّل `assets/app.js`، و`/api/catalog`، وبثّ صوت `/audio/<sha>.ogg`، و**سياسة الخصوصية** |
| `mwqwf/minbar-adkshk` | تطبيق أندرويد `com.ali.menbaradkshk` (Kotlin/Compose) |
| `mwqwf/minbar-adkshk-admin` | لوحة الإدارة |
| `mwqwf/minbar-cloud` | خادم `minbar-api` على Cloudflare (خاصّ) |

## ⚠️ سياسة الخصوصية هنا هي المُلزِمة أمام Google Play

`privacy.html` في هذا المستودع هو ما يقرؤه المستخدم والمراجع. ووصفُه يجب أن
يطابق **شيفرة التطبيق** في `mwqwf/minbar-adkshk` لا ما كان عليه سابقاً: الأذونات
في `AndroidManifest.xml`، والمعالجون في الاعتماديات فعلاً.

**درسٌ وقع فعلاً (2026-09-13)**: كانت في مستودع التطبيق نسخةٌ من صفحات الموقع
(`site/`) لا تُنشر، فصُحّحت فيها السياسةُ ظنّاً أنّها الحيّة — ولم ينتفع بها
أحد. حُذفت تلك النسخة، ووُضع في مستودع التطبيق حارسٌ يمنع عودتها.

⛔ فالقاعدة: **تحقّق من مصدر النشر قبل أن تُصلح شيئاً منشوراً**، و**اقرأ إصلاحك
من العنوان المنشور** (نشرة المعاينة) لا من المحرّر — فالمعاينة في تلك الواقعة
كشفت ثلاثة مواضع ظُنَّت مُصلَحة وهي لم تُمسّ.

## مصدر البيانات

`api/_lib.js`: الترتيب **minbar-api** (Cloudflare Workers + D1) ← لقطة احتياطية
على jsDelivr ← Firestore REST (بقيّةٌ قديمة تُطفأ عند إغلاق مشروع Firebase).
ولا أسرار في المستودع: مفتاح الويب علنيّ بحكم تصميم Firebase.

## ⛔ انتبه قبل التعديل

- `vercel.json` يوجّه `/assets/*` إلى **jsDelivr ببصمة إيداعٍ مثبَّتة**. فتعديل
  `assets/app.js` أو `app.css` لا يظهر للناس حتى تُحدَّث تلك البصمة في المسار.
- الدوال الخادمية في `api/` تعمل على `@vercel/node`، و`transcode.js` يحمل
  ffmpeg وله حدّ زمنيّ وذاكرة في `vercel.json` — لا تُغيَّر بلا سبب.
- `.well-known/assetlinks.json` يربط روابط `/lesson/<id>` بتطبيق أندرويد.
  حذفُه أو تغييرُ بصمته يكسر فتح الروابط في التطبيق.

## ⛔⛔ جلستُك لا تُشغّل سيرَ عمل — والزنادُ هو الدفع

`gh workflow run` ممنوعٌ على جلسات كلود (منعٌ من منصّة أنثروبيك لا من صلاحيّات
المستودع). وهنا لا تحتاجه أصلاً: **الدفعُ إلى `main` ينشر الموقع** عبر Vercel من
تلقائه، ونشرةُ المعاينة تُبنى لكلّ طلب سحب. فاقرأ إصلاحك من العنوان المنشور.

وفي بقيّة مستودعات منبر الزنادُ كذلك دفعٌ لا ضغطة: رفعُ `versionCode` ينشر
التطبيق واللوحة، ودفعُ `src/**` ينشر الخادم، وطلبُ سحبٍ يمسّ `app/src/**` يُقلع
المحاكي. والدليل الكامل في `mwqwf/minbar-adkshk` ← `docs/CLOUD-SESSION.md`.

## ⛔ ولا تقل «يلزم حاسوب المالك»

لا شيء في منبر يحتاج جهازه: البناء والتوقيع والنشر والمحاكي والخادم والموقع
كلّها سحابيّة (متحقَّق 2026-09-13). وإن عجزتَ فالسبب سرٌّ ناقص باسمه، أو صلاحيةُ
رمزك لا تبلغ مستودعاً آخر، أو حدٌّ من المنصّة — سمِّه بعينه ولا تُحِل على جهازه.
